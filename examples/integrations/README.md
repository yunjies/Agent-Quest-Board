# Integration Examples｜真实接入案例

这里放真实平台接入案例，但所有配置都必须脱敏并可替换。

当前案例：

```text
codex-principal/      Codex 作为甲方
hermes-contractor/    Hermes/多多作为乙方
duoduo-board/         多多承载公告板运行时
lark-topic-board/     飞书话题作为公告板前端
```

每个案例尽量包含：

- `README.md`
- `component.example.json`
- `config.example.yaml`
- `sample-task.json`
- `sample-events.jsonl`
- `run-smoke.ps1`
- `run-smoke.sh`

真实配置只存在本地，不进入仓库。
