# Minimal Examples｜最小协议样例

这里放平台无关的最小样例，用于理解协议本身。它们不包含 Codex、Hermes、Lark、AgentOps 或任何真实平台信息。

建议新接入方先读：

1. `principal.example.json`
2. `contractor.example.json`
3. `board.example.json`
4. `task.example.json`
5. `event-log.example.jsonl`
6. `no-frontend-lifecycle.md`

这些样例可以作为测试 fixture 或新适配器开发时的参考输入。
